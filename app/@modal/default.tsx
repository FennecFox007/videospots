// Default content for the @modal parallel-route slot. When no intercepting
// route matches the current URL, this renders nothing — so the slot is
// effectively closed.
export default function ModalDefault() {
  return null;
}
